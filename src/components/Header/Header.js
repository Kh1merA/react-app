import './header.css';

function Header(){
    return  (
        <header className='header'>
            <span>Menu</span>
            <span>Logo</span>
            <span>User Photo</span>
        </header>
    );
}

export default Header;